{
  "name": "image-classification",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "A command-line tool for classifying images as either daytime or nighttime photos. It uses a threshold-based approach to determine the average brightness of the image and categorize it accordingly.",
  "dependencies": {
    "axios": "^1.7.9",
    "cli-progress": "^3.12.0",
    "jimp": "^0.16.1"
  }
}
